<?xml version="1.0" encoding="UTF-8" ?>
<!--
Copyright (C) 2015 Baa. All rights reserved.
See LICENSE.txt for this sample’s licensing information
-->
<document>
  <menuBarTemplate>
    <menuBar>
      
      <menuItem id="All" 
          onSelect="Presenter.loadMenuContent('Movie_Directory', '{{PMSID()}}', '{{PATH(all)}}')"
          >
        <title>{{TEXT(All Movies)}}</title>
      </menuItem>
      
      <menuItem id="OnDeck"
          autoHighlight="true"
          onSelect="Presenter.loadMenuContent('Movie_OnDeck', '{{PMSID()}}', '{{PATH()}}')"
          >
        <title>{{TEXT(On Deck)}}</title>
      </menuItem>
      
      <menuItem id="Unwatched"
          onSelect="Presenter.loadMenuContent('Movie_Directory', '{{PMSID()}}', '{{PATH(unwatched)}}')"
          >
        <title>{{TEXT(Unwatched)}}</title>
      </menuItem>
      
      <menuItem id="Collections"
          onSelect="Presenter.loadMenuContent('Movie_Directory', '{{PMSID()}}', '{{PATH(collection)}}')"
          >
        <title>{{TEXT(Collections)}}</title>
      </menuItem>
      
      <menuItem id="Genre"
          onSelect="Presenter.loadMenuContent('Movie_Directory', '{{PMSID()}}', '{{PATH(genre)}}')"
          >
        <title>{{TEXT(Genre)}}</title>
      </menuItem>
      
      <menuItem id="ByFolder"
          onSelect="Presenter.loadMenuContent('Movie_Directory', '{{PMSID()}}', '{{PATH(folder)}}')"
          >
        <title>{{TEXT(By Folder)}}</title>
      </menuItem>
      
      <!--
      todo: /decade /director /actor more...(secondary)
      -->
      
    </menuBar>
  </menuBarTemplate>
</document>
