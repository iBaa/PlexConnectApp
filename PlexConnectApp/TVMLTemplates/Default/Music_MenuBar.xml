<?xml version="1.0" encoding="UTF-8" ?>
<!--
Copyright (C) 2015 Baa. All rights reserved.
See LICENSE.txt for this sample’s licensing information
-->
<document>
  <menuBarTemplate>
    <menuBar>
      
      <menuItem id="Albums"
          onSelect="Presenter.loadMenuContent('Music_Directory', '{{PMSID()}}', '{{PATH(albums)}}')"
          >
        <title>{{TEXT(Albums)}}</title>
      </menuItem>
      
      <menuItem id="Artists"
          autoHighlight="true"
          onSelect="Presenter.loadMenuContent('Music_Directory', '{{PMSID()}}', '{{PATH(all)}}')"
          >
        <title>{{TEXT(Artists)}}</title>
      </menuItem>
      
      <menuItem id="RecentlyAdded"
          onSelect="Presenter.loadMenuContent('Music_Directory', '{{PMSID()}}', '{{PATH(recentlyAdded)}}')"
          >
        <title>{{TEXT(RecentlyAdded)}}</title>
      </menuItem>
      
      <menuItem id="Genre"
          onSelect="Presenter.loadMenuContent('Music_Directory', '{{PMSID()}}', '{{PATH(genre)}}')"
          >
        <title>{{TEXT(Genre)}}</title>
      </menuItem>
      
      <!--
      todo: /decade /year (secondary)
      -->
      
    </menuBar>
  </menuBarTemplate>
</document>
