<?xml version="1.0" encoding="UTF-8" ?>
{{XML()}}
{{
<document>
    <head>
        <style>
            .lightBackgroundColor {
            background-color: #85837e;
            }
        </style>
    </head>
    <alertTemplate theme="dark" class="lightBackgroundColor">
    
        <title>Sort Music:</title>
         <description>This should be actually auto populated with appropriate section filters...</description>
        <button onSelect="Presenter.load('Movie_Grid', '{{PMSID()}}', '{{PATH(all)}}')">
            <text>{{VAL(MediaContainer/title1)}}</text>
        </button>
        <button onSelect="Presenter.load('Genres', '{{PMSID()}}', '{{PATH(genre)}}')">
            <text>By Genre</text>
        </button>
        
    </alertTemplate>
</document>
}}