<?xml version="1.0" encoding="UTF-8" ?>
<!--
Copyright (C) 2015 Baa. All rights reserved.
See LICENSE.txt for this sample’s licensing information
-->
<document>
    <head>
        <style>
            .lightBackgroundColor {
            background-color: #85837e;
            }
        </style>
    </head>



    <alertTemplate theme="dark" class="lightBackgroundColor">
        <title>Please choose a Plex Media Server</title>
         <description></description>
{{PMSLOOP(owned)}}
{{
{{XML()}}
{{
        <button onSelect="Presenter.load('Library', '{{PMSID()}}', '{{PATH(/library/sections/)}}')">
            <title>{{PMSVAL(name)}}</title>
        </button>
}}
}}		
{{PMSLOOP(shared)}}
{{
{{XML()}}
{{
        <!--button onSelect="Presenter.load('SharedLibraries', '{{PMSID()}}', '{{PATH(/library/sections/)}}')">
            <title>{{PMSVAL(name)}}</title>
        </button--> <!--Does not work -->
        <button onSelect="Presenter.load('Library', '{{PMSID()}}', '{{PATH(/library/sections/)}}')">
            <title>{{PMSVAL(name)}}</title> <!-- At least lists shared server sections -->
        </button>
}}
}}        
    </alertTemplate>
</document>

