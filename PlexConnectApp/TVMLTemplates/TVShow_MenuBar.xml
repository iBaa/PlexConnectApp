<?xml version="1.0" encoding="UTF-8" ?>
<!--
Copyright (C) 2015 Baa. All rights reserved.
See LICENSE.txt for this sample’s licensing information
-->
<document>
  <menuBarTemplate>
    <menuBar>
        
      <menuItem id="All"
          onSelect="Presenter.loadMenuContent('TVShow_ShowList', '{{PMSID()}}', '{{PATH(all)}}')"
          >
        <title>{{TEXT(All Shows)}}</title>
      </menuItem>
      
      <menuItem id="OnDeck"
          autoHighlight="true"
          onSelect="Presenter.loadMenuContent('TVShow_OnDeck', '{{PMSID()}}', '{{PATH()}}')"
          >
        <title>{{TEXT(On Deck)}}</title>
      </menuItem>
      
      <menuItem id="Unwatched"
          onSelect="Presenter.loadMenuContent('TVShow_ShowList', '{{PMSID()}}', '{{PATH(unwatched)}}')"
          >
        <title>{{TEXT(Unwatched)}}</title>
      </menuItem>
      
      <menuItem id="Genre"
          onSelect="Presenter.loadMenuContent('TVShow_Directory', '{{PMSID()}}', '{{PATH(genre)}}')"
          >
        <title>{{TEXT(Genre)}}</title>
      </menuItem>
      
      <!--
      <menuItem id="More"
          onSelect="Presenter.loadMenuContent('TVShow_Secondary', '{{PMSID()}}', '{{PATH()}}')"
          >
        <title>{{TEXT(More...)}}</title>
      </menuItem>
      -->
      
    </menuBar>
  </menuBarTemplate>
</document>
